# Branched-List-of-User-Specified-Hierarchies

TASK LIST:

- Add tabulations based on level
- Button to create new node on top
- Button to create new node on selection
- Button to delete selected node
- Button to delete selected node and childs
- Do upTree downTree
- Set child node under root
- Set child node and all childs under root
- Set child node under node
- Set child node and all childs under node

When an action has been taken, and select is enabled, hide all other buttons, and add a cancel action one
Can we make tree nodes that are empty be text instead of a tree node, or something?
Memory leaks?

